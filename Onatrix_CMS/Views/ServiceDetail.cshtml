@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ServiceDetail>
@{
	Layout = "mainLayout.cshtml";
}

<partial name="Partials/Layout/_PageHeader" model="(IPageHeader)Model" />

<section class="py-16">
	<div class="max-w-7xl mx-auto px-4">
		<div class="flex flex-row gap-8">
			<aside class="w-1/4 flex flex-col gap-4">@await Html.GetBlockListHtmlAsync(Model.ServiceDetailAsides)
			</aside>

			<div class="block w-px bg-gray-200"></div>

			<div class="flex-1 flex flex-col gap-2">
				Content

			</div>
		</div>
	</div>
</section>